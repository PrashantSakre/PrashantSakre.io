class SpeedFactor {
  constructor(factor) {
    this.factor = factor;
  }

  setSpeedFactor(factor) {
    this.factor = factor;
  }

  getSpeedFactor() {
    return this.factor;
  }
}

export const speedFactor = new SpeedFactor();
